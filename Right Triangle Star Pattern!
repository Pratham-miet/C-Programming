#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main(){
    int row,coll,n;
    scanf("%d", &n);
    for(row=1; row<=n; row++){
        for(coll=1; coll<=row; coll++){
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}
