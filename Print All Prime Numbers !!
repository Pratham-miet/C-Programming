#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int i, start, end;
int isprime(int num){
    int factor=0;
    if(num<2){
        return 0;
    }
    for(i=1; i<=num; i++){
        if(num%i==0){
            factor++;
        }
    }
    if(factor==2){
        return 1;
    }
    else{
        return 0;
    }
}
int main() {
    int found=0;
    scanf("%d %d", &start, &end);
    for(int i=start; i<=end; i++){
        if(isprime(i)){
            printf("%d ", i);
            found=1;
        }
    }
    if(!found){
        printf("No prime numbers found");
    }
    return 0;
}
