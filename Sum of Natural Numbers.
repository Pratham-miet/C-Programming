#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main(){
    int i,N,Sum=0;
    scanf("%d", &N);
    for(i=1; i<=N; i++){
        Sum+=i;
        
    }
    printf("%d", Sum);
    
    return 0;
}
