#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n;
    scanf("%d", &n);
    int i=1;
    while(i<=n){
        if (n%i==0){
            printf("%d ", i);
        }
        i=i+1;
    }
    return 0; 
}
