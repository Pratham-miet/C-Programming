#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int i=1,n;
    scanf("%d", &n);
    while(i<=n){
        if (n%i==0){
            printf("%d ", i);
        }
        i=i+1;
    }
    return 0; 
}
